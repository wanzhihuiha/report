TestNG testNG = new TestNG();
使用的时候提示testng不能识别，然后把依赖和在maven里的testng的包都删掉，然后再重新导入

把各个功能结合后去运行一直提示java.lang.ClassNotFoundException
实例化对象失败了。但是还没发现为什么，在其他项目里就是正常的，在这边就不行
原因是因为生成的.java文件被放在文件夹中，不能这样放，应该直接放在包下面。
比如直接放在src下

简单测试系统基本功能都实现了。现在还需要把testng的报告也自动显示出来。
想了一下大概思路：
在页面上点完运行后，在用例后面显示一个链接，点击后就显示测试报告。
（考虑是否要创建数据库，如果创建数据库的，至少三个字段：测试用例编号、
测试用例名、和测试报告，其中测试用例编号、测试用例名，一开始就要有。
那么报告的字段应该存个路径。
也就是说每次运行测试用例后就要把测试报告保存下来。并把路径写入数据库
难点就在于怎么保存这个测试报告。

想到个idea!

这个测试报告是生成在本机的话，那个直接把它复制走，复制到本地的文件夹里。、
然后重命名，不能都是index.html
数据库就保存这个名字。
反正路径也是固定的，都不用保存这个路径，到时候读数据库的时候没有运行过的就没有保存
测试报告名，否则就是有 运行。那就用a标签显示这个测试报告名
